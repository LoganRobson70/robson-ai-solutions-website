# Dirty Release Assessment - 2026-05-30

Repo: `/Users/wayne/Documents/RobsonAI/Codex App/Robson AI Solutions Website`
Branch: `codex/professionalize-website`

## Summary

The remaining dirty worktree is a broad website release candidate, not part of the Codex baseline/auth PR.

Recommendation: keep it out of the baseline PR and handle it as a separate release tranche after review.

## Current Dirty Groups

### Release candidate site files

These appear to belong together if Wayne approves a separate website release commit:

- `index.html`
- `styles.css`
- `privacy.html`
- `netlify.toml`
- `holding.html`
- `building-analyst.html`
- `who-its-for.html`
- `preview.html`
- `script.js`
- `robots.txt`
- `sitemap.xml`

Reason: these files collectively implement the current public holding page, hidden fuller site, protected subpages, consent/analytics behaviour, robots/sitemap stance, and Netlify route/auth setup.

Risk if split incorrectly: the public root, hidden routes, preview auth, sitemap/robots, and QA harness can drift and create broken routes or accidental public exposure.

### Release assets

These should be reviewed and likely staged with the website release candidate if they are referenced by the HTML/CSS:

- `assets/building-analyst-draft-view.png`
- `assets/building-analyst-output-generate.png`
- `assets/building-defect-example.jpeg`
- `assets/fonts/fraunces-latin.woff2`
- `assets/fonts/manrope-latin.woff2`
- `assets/og/robsonai-cover-1200x630.png`
- `assets/og/robsonai-cover-source.html`
- `assets/robson-ai-icon-new.png`
- `assets/robson-ai-icon-v3-transparent.png`
- `assets/robson-ai-icon-v3.png`

Reason: the site references new imagery, fonts, Open Graph assets, and icon variants. Missing asset files would cause broken visual or metadata output.

### Release support docs

These appear useful and should be committed with, or just before, the website release candidate:

- `docs/measurement-qa.md`
- `docs/measurement-qa-closeout-2026-03-19.md`
- `docs/website-narrative.md`
- `UI_UX_REVIEW_2026-04-11.md`

Reason: they document analytics governance, narrative constraints, evidence history, and UX risks.

### Lockfile generated by Netlify/Edge tooling

- `deno.lock`

Recommendation: include only if Netlify Edge Function deploy or local Netlify tooling requires it reproducibly. It appears generated from Netlify Edge bootstrap plus package dependencies, so review with the release deploy workflow before staging.

## Proposed Next Release Tranche

Name: `website-release-candidate-review`

Scope:

- Inspect all remaining dirty website files.
- Confirm every referenced asset exists.
- Run local browser QA across desktop and mobile.
- Run `npm run qa:measurement:local`.
- Run `npm run qa:measurement:evidence` if local smoke passes.
- Run preview auth live verification on a non-production Netlify alias.
- Confirm public root stays holding-only and hidden routes stay protected.
- Stage and commit the release files only after validation.

Out of scope:

- Production deploy.
- Public launch.
- GA4 Measurement ID enablement.
- Contact form implementation.
- Domain/DNS changes.

## Recommendation

1. Recommended: run `website-release-candidate-review` before staging any of these dirty files.
2. Do not mix these files into the Codex baseline/auth PR.
3. Do not production deploy from this dirty worktree until the release candidate review passes and Wayne approves production release.

## Review Result - 2026-05-30

Status: passed with targeted CSS fixes.

Changes made during review:

- Moved the consent banner on hidden preview/product pages so it no longer covers desktop hero text.
- Moved the holding-page desktop consent banner so it no longer covers the hero visual text.
- Fixed mobile horizontal overflow on the hidden fuller-site page.
- Bumped the stylesheet query string on release pages to `styles.css?v=20260530a` so corrected CSS is fetched on preview/deploy.

Validation evidence:

- `npm run qa:measurement:local` passed.
  - Artifact: `output/measurement/smoke-2026-05-30T14-08-10-518Z`
- `npm run qa:measurement:evidence` passed.
  - Artifact: `output/measurement/evidence-2026-05-30T14-08-22-954Z`
  - Lighthouse median run: performance `80`, accessibility `100`, best practices `100`, SEO `66`
- `npx html-validate --rule doctype-style:off --rule void-style:off holding.html index.html preview.html privacy.html building-analyst.html who-its-for.html` passed.
- `git diff --check` passed.
- Asset reference scan found no missing local asset references.
- Non-production Netlify alias deploy passed:
  - URL: `https://release-candidate-check--robson-ai-website.netlify.app`
  - Deploy id: `6a1aef9300af4ff50eff277f`
  - `/` returned public holding page, no hidden fuller-site links.
  - `/index.html`, `/building-analyst.html`, `/who-its-for.html`, and `/privacy.html` returned `401` without/wrong credentials and `200` with the Keychain preview credential.
- Browser checks passed:
  - Desktop holding root: correct title/H1, no hidden links, no console errors/warnings, no horizontal overflow.
  - Mobile holding root at `390x844`: no hidden links, no console errors/warnings, no horizontal overflow.
  - Hidden fuller-site desktop: consent banner no longer overlaps the H1 or hero visual.
  - Hidden fuller-site mobile at `390x844`: no horizontal overflow.

Remaining known limitations:

- Production deploy/public launch remains out of scope.
- GA4 remains intentionally inert because the Measurement ID is empty.
- Lighthouse SEO remains `66`, expected while hidden/private pages are `noindex` and the public root is a holding page.
- Local HTTP browser clipboard can show fallback copy behaviour; the Playwright smoke validates the copy success path using a clipboard stub.

Release recommendation:

1. Recommended: stage and commit the release candidate files as a separate website release commit.
2. Keep PR #1 as draft until this release candidate commit is either added to it intentionally or split into a separate PR.
3. Do not production deploy until Wayne explicitly approves production release after reviewing the non-production alias.
